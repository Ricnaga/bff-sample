export class Sample {
  id: string;
  nome: string;
  sobrenome: string;

  constructor(id: string, nome: string, sobrenome: string) {
    this.id = id;
    this.nome = nome;
    this.sobrenome = sobrenome;
  }
}